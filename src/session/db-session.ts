import { generateKeyPair } from "crypto";

function generate(userIdx: number) {
}

function isValid(accessToken: String) {
}

function remove(accessToken: String) {
}

function getAll(userIdx: number) {
}
